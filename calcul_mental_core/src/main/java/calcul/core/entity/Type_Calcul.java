package calcul.core.entity;

public enum Type_Calcul{
    //Les différents types de calculs existants sont mis dans un enum pour éviter les erreurs de saisie
    Soustraction,
    Addition,
    Multiplication,
    Division;
}
